Files here are copied verbatim to the site root by tools/build_site.py.
Use for search-engine verification files (Google Search Console, Bing) and
anything else that must be served byte-for-byte at a fixed URL.

